export enum AppType { SYSTEM = 'SYSTEM', USER = 'USER' }
export interface AppConfig { id: string; name: string; icon: string; color: string; sourceCode?: string; }
export interface WindowState { id: string; appId: string; title: string; isMaximized: boolean; isMinimized: boolean; zIndex: number; icon: string; initialData?: any; orientation?: 'portrait' | 'landscape'; }
export interface SystemFile { id: string; name: string; path: string; type: 'file' | 'folder'; extension?: string; size: number; content?: string; }
export interface OSState { booted: boolean; isLocked: boolean; isOff: boolean; isSuspended: boolean; authorizedUser: string; isSecurityConfigured: boolean; manualBypassAllowed: boolean; biosStage: number; wallpaper: string; ssdUsed: number; ramUsed: number; windows: WindowState[]; activeWindowId: string | null; files: SystemFile[]; processes: any[]; services: any[]; installedApps: AppConfig[]; githubRepo?: string; githubToken?: string; isDevMode: boolean; audioSettings: any; allowUnknownSources: boolean; restorePoint: any; externalDriveLinked: boolean; driveUrl: string | null; agencyBrain?: any; systemDirty?: boolean; }